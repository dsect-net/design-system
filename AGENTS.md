# Agents working in this repo

Read `CONTRIBUTING.md`; everything there applies. This file is the short version, plus what agents in
particular get wrong.

## Before you open a pull request

```bash
npm run check                      # must exit 0
npm install && npm run smoke       # must exit 0 — renders every page
cd react && npm run typecheck && npm run build   # if you touched react/ or any CSS class
```

Report the numbers you measured, not "should work". Send screenshots of both themes and a phone width for anything visual.

## Rules

- **Values live in `tokens.css` only.** Everything else refers to them with `var()`. The check fails on a literal.
- **Class names are an API** (the hub and `@dsect/ui` use them). Add, alias or fix; never rename.
- **Write "DSECT".** Never `D//SECT`, `D-SECT` or `D/SECT`. `DSECT//` is the visual wordmark only, and its `//`
  is always `aria-hidden="true"`.
- **Do not draw, generate or "improve" logo marks.** Logo artwork is image-generated by the founder and dropped into
  `brand/`. Wire in the files; don't make them.
- **Instruments get a bare codename** (Nebula, Reeve, Quantum). Only things outside people see get the DSECT
  prefix. Brand changes need an ADR; you can propose, not decide.
- **Tritium is the machine; Quantum is the AI system on it.** Never find-and-replace one for the other.
- **Public repository.** Sample data only: no tailnet names, hosts, keys, emails, internal paths or security
  posture. Ring-fenced brands do not appear here at all.
- **44px hit areas on touch, both themes, no sideways scroll at 360px.** The smoke test measures all three.
- **In Tailwind / Untitled UI apps**, import `base.css` and `components.css` into CSS layers
  (`untitled/README.md`). Unlayered, they override every utility.

## Where things are

| Path | What |
|---|---|
| `tokens.css` · `fonts.css` · `base.css` · `components.css` | the system, in load order |
| `previews/`, `index.html` | the gallery; one page per category |
| `templates/` | copy-paste starting pages for apps |
| `react/` | `@dsect/ui`, typed wrappers over the same classes |
| `untitled/` | the Untitled UI React theme bridge (TRELLIS) |
| `brand/` | name rules, logo artwork slots, archive |
| `docs/proposals/` | decision memos |
| `scripts/` | `check.mjs` (source) and `smoke.mjs` (rendered) |
